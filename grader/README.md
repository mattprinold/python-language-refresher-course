# Grader (spoilers)

`solutions.py` contains reference implementations for all exercises.

**Try the exercises yourself before opening it.**

The test suite in `tests/` is the source of truth — your implementations pass when all 55 tests are green.

```bash
python grader/check.py
# or
pytest tests/ -v
```
